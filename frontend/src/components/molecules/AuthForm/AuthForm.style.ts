import styled from 'styled-components';
import { Box } from '@mui/material';
import { CSSProperties } from 'react';

export const LoginText: CSSProperties = {
  textAlign: 'center',
  marginBottom: '1.5rem',
};

export const LoadingBtn: CSSProperties = {
  paddingTop: '0.8rem',
  paddingBottom: '0.8rem',
  marginTop: 2,
  width: '80%',
  marginInline: 'auto',
};
